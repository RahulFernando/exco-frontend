import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h6">Home</Typography>
    </div>
  );
};

export default Home;
